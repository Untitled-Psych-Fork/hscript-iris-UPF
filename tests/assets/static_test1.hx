static var allRight = "Come Heaven";
static final allBad = "Go on The Hell";

function new() {
	// reading
	trace("static_test2 static var fan: " + fan);
	var old = sad;
	sad += " forever";
	trace("static_test2 static var sad: " + sad);
	trace("writing sad compare: [" + old + "<=>" + sad + "]");
}
